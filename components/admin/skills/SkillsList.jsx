import React from 'react'
import Skill from './Skill'

const SkillsList = ({ skill }) => {
    return (
        <div>
            <Skill skill={skill} />
        </div>
    )
}

export default SkillsList